/**
 * Aqui é manipulado toda a parte de upload das imagens
 */
import v2 from './save-02-fotos-v2';
export const seletor = '.c-criar-anuncio.a-index';

export const callback = ($) => {
    v2($);
};
