import React, { Component, PropTypes } from 'react';
import ReactDOM from 'react-dom';

class NoMsgs extends Component {

    render() {
        return (
                <div className="text-center">
                    Você ainda não tem nenhuma conversa!!!!!
                </div>
                );
    }
}

export default NoMsgs;